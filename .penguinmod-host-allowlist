# Files exempt from the "no upstream hosts" CI guard.
# gitignore-style path globs; one per line. Only tooling that intentionally
# references the upstream host to REWRITE it, migration provenance, and human
# documentation belong here. Application/runtime code must never be hand-added.

# Documentation legitimately names the upstream project and its hosts.
**/*.md
**/*.mdx

# --- Runtime host debt CLOSED 2026-06-21 -------------------------------------
# The baseline-seeded runtime files (routes, Logs.js, UserManager.js, vercel.json,
# 404-noproject.html, email templates) have been repointed to patternyard.dev and
# REMOVED from this allowlist, re-arming the guard. Any future upstream merge that
# reintroduces a *.penguinmod.com runtime host into these files will now FAIL CI
# (by design) and must be repointed per the canonical host map.
